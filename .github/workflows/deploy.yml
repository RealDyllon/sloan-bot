name: Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-image:
    name: Build Docker Image
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: lowercase-ify REPO name
        run: |
          echo "repository_lowercase=${github.repository}" | tr '[:upper:]' '[:lower:]' >> ${GITHUB_ENV}

      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          file: ./Dockerfile
          context: .
          push: true
          tags: |
            ghcr.io/${{ env.repository_lowercase }}:${{ github.sha }}
            ghcr.io/${{ env.repository_lowercase }}:latest
